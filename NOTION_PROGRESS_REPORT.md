# GridLootMaster 개발 진행 보고

작성 기준일: 2026-09-04  
엔진: Unreal Engine 5.7  
현재 상태: Phase 6 기본 구현 완료 / 실제 에디터 플레이 검증 대기

## 1. 한눈에 보는 진행 상황

| 기준 | 진행도 | 상태 |
|---|---:|---|
| Phase 4~6 코드·자동화 구현 | 약 92% | 핵심 시스템과 주요 오류 수정 완료 |
| Phase 6 | 약 93% | 전투 백엔드, 레이드 루프, 탈출지점, HUD 기본 흐름 구현 |
| 최종 완료 기준 | 약 85~90% | 실제 UI 플레이·저장 재시작 검증이 남음 |

진행률은 엔진이 제공하는 공식 수치가 아니라, 구현 범위·자동화 테스트·사용자 검수 잔여량을 기준으로 한 추정치다.

## 2. 이번 최신 작업

- Lobby 상태에서 Stash 아이템을 이동·정리하면 자동 저장되도록 연결했다.
- 성공 추출 후 장비와 운반 아이템을 자동으로 Stash에 넣거나 비우지 않고 현재 위치에 유지한다.
- 성공 추출 후 Lobby로 돌아오며 Stash와 `START RAID`를 다시 사용할 수 있다.
- 실패·사망·시간초과 후에도 결과를 표시한 뒤 Lobby로 복귀해 다음 레이드를 준비할 수 있다.
- 테스트마다 GUID 기반 임시 SaveGame 슬롯을 사용하도록 바꿔 실제 사용자 세이브와 테스트가 충돌하지 않게 했다.

## 3. 구현된 주요 시스템

### 인벤토리·장비

- 그리드 배치, 이동, 회전, 부분·전체 스택 병합 및 분할
- 장비 교체 실패 시 기존 장비 복구
- Chest Rig 장비 슬롯 `2x1`, Backpack 장비 슬롯 `2x3`
- Backpack 내부 그리드는 기존 `5x6` 유지
- 무기 부착물, 탄창, 탄약 호환성 및 언로드 처리
- Inspect/Examine 상태와 관련 UI 갱신

### 레이드·맵·탈출

- `Lobby`, `InRaid`, `Succeeded`, `Failed` 상태 분리
- 9x9 타일 맵, 벽/장애물 기반 이동 가능성 검사, A* 경로 탐색
- 전체 맵과 미니맵의 목적지·경로 상태 공유
- 이동 중 경로 변경 잠금 및 이동 직전 벽 재검증
- 스폰 위치 기준 반대편 후보를 우선하는 탈출지점 생성
- 최소 거리와 경로 유효성 기반 탈출지점 판정
- 탈출지점 타일 접근 전 `EXTRACT RAID` 차단

### 전투·알림

- 적 HP, 공격력, 명중률, 방어력, 보상과 기본 공격·반격·처치 흐름
- 이동 완료 후 확률 기반 적 인카운터 생성
- 상단 이벤트 알림 및 레이드 결과 알림의 기본 연결

## 4. 검증 결과

- Unreal Engine 5.7 Development Editor 빌드 성공
- Stash 자동화 테스트: 11/11 통과
- 전체 자동화 테스트: 68/68 통과
- 자동화 결과: 실패 0, Critical Error 0, 종료 코드 0
- DataTable 재임포트: `DT_ItemData` 0 Problems
- `git diff --check`: 통과
- 테스트 중 실제 사용자 Stash 저장 파일은 덮어쓰지 않음

## 5. 사용자 검수 필요 항목

에디터를 실행할 수 있을 때 다음 순서로 확인한다.

1. Lobby에서 장비와 아이템을 Stash에 배치한다.
2. `START RAID`로 레이드에 진입한다.
3. 전체 맵과 미니맵에서 타일을 클릭하고 목적지 하이라이트와 전진 버튼 상태를 확인한다.
4. 전진 버튼으로 목적지까지 이동하고, 벽/장애물 타일에서 이동이 막히는지 확인한다.
5. 탈출지점이 아닌 곳에서 `EXTRACT RAID`가 차단되는지 확인한다.
6. 탈출지점에서 추출한 뒤 장비·운반 아이템이 그대로 남고 Stash와 `START RAID`가 다시 활성화되는지 확인한다.
7. Lobby에서 아이템을 다시 정리한 뒤 에디터를 종료하고 재실행해 자동 저장 결과를 확인한다.
8. 사망 또는 시간초과 후 일반 운반품 손실, SafeBox 보존, Lobby 복귀를 확인한다.

## 6. 아직 보류하거나 자료가 필요한 항목

- 실제 맵 디자인 자료를 받기 전까지 현재 9x9 프로토타입 색상·배치·벽 구조는 임시다.
- 아이콘 이미지 에셋과 CSV의 `ItemIcon` 경로가 없어 아이콘 최종 검수는 보류 중이다.
- 주기적 적 스폰, 탐색 능력에 따른 주변 적 감지, WASD 행동 매핑은 전투 기획 검토 후 구현한다.
- 가방을 가진 상태의 특수 탈출 조건, 탈출 비용, 고정 북·남 탈출지점 규칙은 세부 기획 확정 후 반영한다.
- 경제·상점 시스템은 Phase 7 대상이다.

## 7. 다음 작업

1. 사용자의 에디터 플레이 검수 결과를 받아 실제 UI 동작을 재현한다.
2. 문제가 발견되면 한 번에 한 문제씩 수정하고 자동화·컴파일로 회귀 검증한다.
3. 맵 디자인 자료가 제공되면 타일 색상, 장애물, 탈출지점 규칙을 새 디자인에 맞춘다.
4. 전투 인카운터 확정 기획을 반영해 주기·감지·WASD 행동을 별도 단계로 구현한다.

## 8. 이번 작업 세션 자원 참고

- Goal 누적 사용량: 약 11.32M 토큰
- 누적 실행 시간: 약 12시간 1분
- 잔여 토큰: Codex 사용량 조회에서 제공되지 않음(`null`)
- Goal 상태: 일시중지 상태이며, 사용자가 재개하면 이어서 진행 가능

## 9. 권장 커밋 메시지

```text
feat: Phase 5~6 레이드 루프와 인벤토리 안정화 구현

- 인벤토리·장비·탄약·부착물 상태 보존 및 UI 갱신 보완
- Stash 저장·로드, Lobby 자동 저장, 추출·실패 후 복귀 흐름 구현
- 탈출지점·미니맵·전투 기본 흐름과 자동화 테스트 추가
- 기획·기술 구현 보고서와 사용자 검수 메모 정리
```

커밋은 아직 실행하지 않았다. 현재 작업 트리에는 기존 변경분과 새 보고서가 함께 있으므로, 커밋 전 포함 범위를 확인해야 한다.
