# GridLootMaster 전체 개발 계획

## 1. 현재 프로젝트 상태

GridLootMaster는 Tarkov 스타일의 그리드 인벤토리와 탈출형 루팅 게임을 목표로 한다. 현재는 Unreal Engine 5 C++ 기반의 핵심 플레이 프로토타입이 구현되어 있으며, Phase 4의 기능 범위에 해당하는 인벤토리·장비·루팅·탐색·미니맵 기능이 연결되어 있다.

다만 실제 대화형 플레이 테스트가 아직 완료되지 않았으므로, 현재 단계는 **Phase 6 기본 기능 구현 완료 / 안정화 검증 진행 중**으로 정의한다.

## 2. 기술 및 작업 원칙

- 핵심 로직과 UI 바인딩은 C++로 구현한다.
- 블루프린트는 시각 요소와 데이터 에셋 중심으로 사용한다.
- 한 번에 하나의 문제만 수정하고, 수정 직후 정적 검증 또는 런타임 검증을 수행한다.
- 기존 동작과 사용자 변경사항을 보존하며, 관련 없는 리팩터링은 하지 않는다.
- 기능의 완료는 코드 작성이 아니라 컴파일·재현 시나리오·회귀 검증까지 통과한 상태로 정의한다.
- 계획서와 진행 보고는 한글로 작성한다.

## 3. 기능 진행도

| 영역 | 현재 상태 | 판정 |
|---|---|---|
| 그리드 인벤토리 | 아이템 배치, 회전, 드래그, 스택 병합 구현 | 핵심 구현 완료, 회귀 검증 필요 |
| 장비 슬롯 | 부위별 장착 및 교체 구현 | 교체 로직 수정 완료, 런타임 검증 필요 |
| 무기 모딩 | 조준경·총구·탄창 장착 및 분리 구현 | 핵심 구현 완료, 취소·상태 갱신 보완 완료 |
| 탄약 | 삽탄·언로드 구현 | 유실 방지 및 호환성 코드 수정 완료, 데이터 자산 반영 확인 |
| Inspect/Examine | 미식별 표시와 Inspect 구현 | 상태 이벤트 연결 완료, 런타임 검증 필요 |
| 컨테이너 탐색 | Search 지연 및 아이템 공개 구현 | 구현 완료, 회귀 검증 필요 |
| 미니맵/이동 | 타일 맵·클릭 기반 이동·전체/축소 미니맵 상태 공유 구현 | 타일 직접 입력 경로·입력 영역·회귀 검증 완료 |
| 전투 | 적 인카운터, 공격·반격·피해·처치 보상·시간 기반 Combat·무기 전투 스탯·C8 교체/재장전 골격 구현 | C8 최소 골격 포함 전체 자동화 테스트 81개 통과, 실제 UI와 DataTable 수치 검증 필요 |
| 창고/탈출 | Stash 그리드·저장·기본 UI·탈출/실패 흐름 구현 | 탈출점 위치 판정·탈출 후 휴대품 유지·Lobby 복귀 포함 자동화 테스트 실행·통과 |
| 경제/상점 | 미구현 | Phase 7 대상 |

## 4. 기존 백로그 진행도

### 완료했으나 런타임 검증이 필요한 항목

1. 장비 교체 시 기존 장비 유실
   - 새 장비가 차지한 칸을 제외하고 기존 장비의 수납 공간을 계산하도록 수정했다.
   - 기존 장비 수납 실패와 새 장비 장착 실패 시 복구 흐름을 추가했다.

2. Inspect 창에서 탄창 언로드 후 메인 무기 탄약 표시가 갱신되지 않는 문제
   - 아이템 변경 이벤트를 일반 아이템 위젯과 장비 슬롯에 연결했다.
   - 탄약·부착물 상태 변경 시 해당 이벤트를 발생시키도록 보완했다.

### 완료했으나 런타임·에셋 검증이 필요한 항목

3. 장비 슬롯 외형 높이와 백팩 그리드 규격
   - 백팩 내부 그리드는 기존 규격인 `5x6`으로 유지한다.
   - 백팩 장비 슬롯의 가로 폭을 `192x192`에서 `128x192`로 줄여 한 칸을 제거했다.
   - Chest Rig 장비 슬롯은 `128x64`(2x1), Backpack 장비 슬롯은 `128x192`(2x3)로 맞춘다.
   - 기본 M4와 샘플 아이템 배치는 기존 방식으로 복원했으며, 실제 화면에서 장비 슬롯 경계와 배치를 확인해야 한다.

4. 기본 Chest Rig/Backpack 데이터 누락
   - `ItemData.csv`에 `DefaultBackpack`, `DefaultRig` 행을 추가했다.
   - 기본 생성부에 표시명을 보완했다.
   - Unreal `DT_ItemData` 재임포트를 완료하고 주요 행과 `Damage`·`Armor` 필드 반영을 확인했다.

## 5. 추가 코드 안정화 항목

현재 코드리뷰에서 확인되어 수정한 항목:

- 부착물 드래그 취소 시 유실 방지
- 탄창 언로드 중 인벤토리가 가득 찰 때 탄약 보존
- 부분 스택 병합 결과 처리 오류 수정
- 탄약 종류 호환성 검사 추가
- 아이템 상태 변경 후 관련 UI 갱신 연결
- 병합 불가 드롭을 성공으로 처리하던 경로 수정
- 그리드 재초기화 시 아이템 맵이 남던 상태 불일치 수정
- 루팅 좌표·가중치의 하드코딩 및 0 가중치 입력 보완
- 승리 조건이 `QuotaScore` 대신 1000에 고정되던 문제 수정
- 제한 시간 종료 순간 UI에 `-1초`가 표시될 수 있던 순서 수정

검증이 필요한 추가 항목:

- Unreal Engine 실제 컴파일 — `C:\UE_5.7` BuildTool로 Development Editor 빌드 성공
- DataTable CSV 재임포트 결과 — 완료 (`Imported DataTable 'DT_ItemData' - 0 Problems` 로그와 자산 행 반영 확인)
- 인벤토리·장비·부착물 간 이동 회귀 테스트
- 아이템 데이터와 UI 아이콘 경로 연결
- 판매 점수 계산이 아이템 가치와 스택 수량을 반영하는지 확인
- 현재 `RawAssets/Icons` 폴더와 실제 아이콘 파일이 없고 CSV의 `ItemIcon` 값이 비어 있어, 아이콘 표시 자체는 이미지 에셋 결정 후 검증한다.

## 6. 개발 순서

### 단계 A — Phase 4 안정화

1. Unreal Engine 컴파일 환경 확인
2. 현재 수정사항 컴파일
3. DataTable CSV 재임포트 — 완료
4. 장비 교체·언로드·스택 병합·Inspect 회귀 테스트
5. 아이콘·판매 점수 등 잔여 결함 정리

검증 진행:

- UnrealBuildTool/UHT 및 `GridLootMasterEditor Win64 Development` 실제 빌드 성공
- `InspectWidget.cpp`의 UE 5.7 deprecated 폰트 생성자 경고 수정 후 재빌드 성공
- `DT_ItemData.uasset`에 CSV의 주요 행과 `Damage`·`Armor` 필드가 반영되었다.
- 재임포트 전 원본은 `DT_ItemData.uasset.preimport.bak`로 보존했다.

완료 기준:

- 컴파일 성공
- 기존 핵심 기능의 재현 테스트 통과
- 아이템 또는 장비가 실패 상황에서 유실되지 않음
- 데이터 에셋과 CSV의 기준이 일치함

### 단계 B — Phase 5: Extraction & Meta Progression

1. 레이드 상태와 로비 상태 분리 — 상태 enum 및 전환 이벤트 구현
2. 영구 보관용 Stash 그리드 구현 — 별도 10x10 컴포넌트·SaveGame·기본 UI 구현
3. 레이드 시작 시 Stash에서 장비·아이템을 선택하는 흐름 구현
4. 탈출구와 Extraction 성공 조건 구현
5. 탈출 성공 후 장비·운반품을 유지한 채 Lobby로 복귀하고, 플레이어가 Stash로 수동 이동
6. 사망 시 SafeBox 보존, 나머지 장비·가방·루팅 아이템 손실 처리
7. 레이드 결과와 Stash 저장 데이터 검증

현재 구현 상태:

- 레이드 추출·실패·사망 처리와 결과 UI 연결까지 코드 구현을 완료했다.
- 성공 추출은 장비·운반품을 자동으로 Stash에 넣거나 삭제하지 않고 Lobby로 복귀하며, Stash 패널과 START RAID를 다시 사용할 수 있다.
- 사망·시간초과는 일반 운반품 손실과 SafeBox 보존을 적용한 뒤 실패 결과를 표시하고 Lobby로 복귀해 다음 출격을 준비할 수 있다.
- 레이드 시작 시 시작점 반대쪽 외곽에 최소 거리·경로 조건을 만족하는 탈출 타일을 생성하고, 현재 좌표가 해당 타일일 때만 추출되도록 연결했다.
- 탈출 타일은 미니맵에서 녹색 `EXIT`로 표시하고, 탈출점 밖에서는 `EXTRACT RAID` 버튼을 비활성화한다.
- 실제 Unreal 실행·저장·재시작 회귀 검증은 아직 남아 있다.

추가 기획 — 탈출지점 규칙:

- `EXTRACT RAID`는 버튼만 누르면 성공하는 방식이 아니라, 플레이어가 실제 탈출 타일에 도착했을 때만 실행할 수 있도록 변경한다.
- 초기 프로토타입은 9x9 맵 외곽의 탈출 후보 중 시작 위치와 반대되는 방향을 우선 선택한다. 예를 들어 서쪽에서 시작하면 동쪽 외곽, 동쪽에서 시작하면 서쪽 외곽을 우선 사용한다.
- 탈출 타일은 시작점으로부터 최소 맨해튼 거리를 보장하고, 후보가 여러 개면 랜덤으로 선택한다. 북쪽·남쪽 고정 탈출구는 후보 규칙으로 확장할 수 있도록 분리한다.
- 탈출 타일은 맵 데이터에 특성으로 표시하고 미니맵에서 일반 타일과 구분한다. 현재 좌표가 탈출 타일이 아니면 탈출 버튼을 비활성화하고 안내 메시지를 표시한다.
- 탈출 시 전투 중인지, 레이드 상태가 유효한지, 현재 좌표가 탈출 타일인지 순서대로 검사한다.
- 가방을 소지하면 탈출할 수 없는 특수 탈출구, 탈출 비용, 특정 아이템 요구 조건은 별도 데이터 규칙으로 추가한다. 가방 제한이 도입될 때는 가방 내부 아이템을 포켓·체스트리그 등으로 옮긴 뒤 가방을 버리는 처리와 실패 시 롤백을 함께 검증한다.

완료 기준:

- 레이드 시작 → 루팅 → 탈출 성공 또는 사망 → 결과 반영의 전체 흐름이 동작함
- 탈출 성공과 사망 시 아이템 보존 규칙이 서로 섞이지 않음
- 재시작 후에도 Stash 상태가 유지됨

### 단계 C — Phase 6: Combat & Enemies

1. 전투 상태와 턴 진행 규칙 정의
2. 적 데이터와 적 인스턴스 구현
3. 기존 랜덤 Encounter 제거 및 Enemy World Model 구축
4. 명중률·피해량·부위별 방어·관통 계산 구현
5. 플레이어 사망 및 전투 종료를 Phase 5 결과 처리와 연결
6. 전투 UI와 로그 표시 구현

현재 구현 상태:

- `UCombatComponent`에 적 정의·인스턴스·공격·반격·처치 보상 상태를 구현했다.
- 전투 개편 C1에서 실제 칸 이동 완료 시 `EncounterChancePercent`로 적을 생성하던 레거시 경로를 제거했다.
- `BANG!` 버튼이 탄약 소비 후 적 공격과 생존 시 반격을 호출하도록 연결했다.
- 전투 상태 변경 이벤트를 메인 UI의 적 상태 표시(체력·처치 상태)와 연결했다.
- 미니맵에서 전진 입력이 성공할 때 GameMode의 Raid World Tick으로 전달하도록 연결했다.
- 후속 기획은 World Tick 기반 Spawn Scheduler, Enemy World Model, 탐색 능력 기반 주변 `N` 범위 감지, 존재·방향·위험도 단계 알림 순서로 진행한다. 정확한 좌표 공개는 제한한다.
- `FItemData`·`UItemInstance`에 피해량 필드를 추가하고 무기 CSV 행에 피해량 25를 반영했다.
- `FItemData`·`UItemInstance`에 방어력 필드를 추가하고 방어구·헬멧의 반격 피해 감소에 연결했다.
- 적 정의에 명중률을 추가하고, 인카운터 적의 반격 명중률을 75%로 설정했다.
- 스택 분할 시 새로 추가한 피해량·방어력 상태가 유실되지 않도록 두 분할 경로를 보완했다.
- 전투 중 이동·컨테이너 탐색·탈출을 차단하고, 레이드 실패·성공 시 전투 상태를 정리하도록 보완했다.
- 메인 UI에 플레이어 체력 표시를 추가하고, 피해 처리 및 레이드 시작 시 현재 체력을 갱신하도록 연결했다.
- 인레이드 안전지대 접근 제한을 적용한다. 레이드 중에는 Stash 패널과 Stash 버튼을 사용할 수 없고, 레이드 시작 시 Stash 패널을 자동으로 닫는다. 로비에서 출격 준비를 하고 탈출·사망·시간초과 후 다시 Stash에 접근하는 흐름을 유지한다.
- 인레이드 HUD에 축소 미니맵과 전진 조작을 항상 표시하고, 토글 미니맵과 동일한 이동 상태를 공유한다. 적 조우로 이동이 중단된 경우를 알림 영역에 명시한다.
- 화면 최상단에 이벤트 알림 영역을 추가해 적 등장, 목적지 선택, 이동 거부 사유, 탈출지점 도착, 이동 중단, 레이드 성공·실패 등의 상태를 텍스트로 전달한다. 전투·이동·레이드 결과 메시지는 2.5초 단위 알림 큐로 순차 표시한다.
- `EXTRACT RAID`가 레이드 외부·탈출점 외부·전투 중 또는 준비 부족으로 거부될 때도 원인별 상단 알림을 표시한다.
- `SEARCH CONTAINER`가 레이드 외부·전투 중 또는 컨테이너 누락으로 거부될 때도 원인별 상단 알림을 표시한다.
- 플레이어 체력 UI 추가 후 실제 Unreal Editor 빌드(UHT·컴파일·링크)가 성공했다.
- 적이 없거나 무기 피해량이 유효하지 않을 때 `BANG!` 버튼이 탄약을 소비하던 순서를 수정하고, 실제 공격 성공 후에만 탄약을 차감하도록 보완했다.
- 탄약 소비 순서 수정 후 실제 Unreal Editor 빌드(UHT·컴파일·링크)가 성공했다.
- 적 생성·방어력 적용·처치·처치 후 공격 거부의 전투 기본 흐름을 `GridLootMaster.Combat.BasicFlow` Unreal 자동화 테스트로 고정했다.
- 전투 기본 흐름 자동화 테스트가 Unreal 무인 실행에서 `Result={Success}`로 완료되고, 테스트 큐 종료까지 확인됐다.
- 부분 스택 병합에서 대상 최대 수량 초과와 원본 잔여 수량 유실을 방지하는 `GridLootMaster.Inventory.PartialMergePreservesRemainder` Unreal 자동화 테스트를 추가했다.
- 부분 스택 병합 회귀 테스트가 Unreal 무인 실행에서 `Result={Success}`로 완료되고, 테스트 큐 종료까지 확인됐다.
- 회전된 아이템의 실제 footprint가 그리드 경계와 점유 셀에 일치하는지 확인하는 `GridLootMaster.Inventory.RotatedFootprintRespectsGridBounds` Unreal 자동화 테스트를 추가했다.
- 회전 footprint 회귀 테스트가 Unreal 무인 실행에서 `Result={Success}`로 완료되고, 테스트 큐 종료까지 확인됐다.
- 교차 인벤토리 이동에서 대상 수납 실패 시 출처와 대상 아이템을 보존하는 `GridLootMaster.Inventory.CrossInventoryMovePreservesSourceOnTargetFailure` Unreal 자동화 테스트를 추가했다.
- 교차 인벤토리 이동 회귀 테스트가 Unreal 무인 실행에서 `Result={Success}`로 완료되고, 테스트 큐 종료까지 확인됐다.
- 장비 슬롯 중복 등록 방지와 해제 후 재장착을 확인하는 `GridLootMaster.Equipment.PreventsDuplicateInstanceAndSlotRegistration` Unreal 자동화 테스트를 추가했다.
- 장비 상태 회귀 테스트가 Unreal 무인 실행에서 `Result={Success}`로 완료되고, 테스트 큐 종료까지 확인됐다.
- 부분 스택 병합 시 대상 인벤토리만 갱신되어 원본 인벤토리 UI가 stale 상태로 남는 경로를 보완했다.
- 부분 스택 병합 UI 갱신 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- GridBoard에서 탄약을 무기 또는 탄창에 삽입할 때 장탄수 변경 이벤트가 누락되어 장착 UI가 stale 상태로 남는 경로를 보완했다.
- GridBoard 탄약 삽입 UI 갱신 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 메인 UI의 활성 무기 슬롯 기본값을 `Primary1`로 설정해 첫 번째 무기 장착 후 숫자 키를 먼저 누르지 않아도 전투 입력이 연결되도록 보완했다.
- 활성 무기 슬롯 기본값 수정 후 실제 Unreal Editor 빌드(UHT·컴파일·링크)가 성공했다.
- 장착 탄창·부착물 해제 시 인벤토리 `AddItem` 실패를 무시하고 슬롯을 비워 아이템이 유실될 수 있던 경로를 보완했다.
- 장착 아이템 해제 유실 방지 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 부착물 교체 드롭에서도 기존 부착물 수납 성공 여부를 확인한 뒤 새 부착물을 장착하도록 보완했다.
- 부착물 교체 드롭 유실 방지 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 일반 장비 우클릭 해제 시 인벤토리 `AddItem` 실패를 무시해 장비가 유실될 수 있던 경로를 보완하고, 실패 시 장비 슬롯으로 롤백하도록 수정했다.
- 일반 장비 해제 유실 방지 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 교차 인벤토리 아이템 이동 시 원본을 먼저 제거해 대상 수납 실패 시 아이템이 유실될 수 있던 순서를 보완했다.
- 교차 인벤토리 이동 유실 방지 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 같은 인벤토리 내부에서 위치 이동 시 대상 수납 실패를 대비해 아이템의 원래 좌표를 저장하고 롤백하도록 보완했다.
- 같은 인벤토리 이동 롤백 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 장비·부착물 등 출처가 없는 드래그에서 모든 인벤토리와 장비를 무조건 제거하던 경로를 보완하고, 실제 원본을 식별한 뒤 대상 수납 성공 시에만 제거하도록 수정했다.
- 출처 미지정 드래그 이동 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 스택 완전 병합 시 모든 컨테이너를 일괄 정리하던 경로를 실제 드래그 출처만 제거하도록 보완했다.
- 스택 완전 병합 출처 정리 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- GridBoard 빠른 부착물 장착 시 대상 무기의 `OnItemModified`가 누락되어 장비·Inspect UI가 stale 상태가 될 수 있던 경로를 보완했다.
- GridBoard 빠른 부착물 UI 이벤트 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 장비 슬롯 빠른 부착물 장착 시 대상 무기의 `OnItemModified`가 누락되어 관련 UI가 stale 상태가 될 수 있던 경로를 보완했다.
- 장비 슬롯 빠른 부착물 UI 이벤트 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 장비 슬롯 탄약 삽입 시 변경된 장탄수를 무기 객체 이벤트로 전파하지 않던 경로를 보완했다.
- 장비 슬롯 탄약 삽입 이벤트 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 탄창 슬롯 탄약 삽입 시 탄창 객체의 `OnItemModified`가 누락되어 탄창 Inspect UI가 stale 상태가 될 수 있던 경로를 보완했다.
- 탄창 슬롯 탄약 삽입 이벤트 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 장비 슬롯 탄창 분리·언로드 후 원본 무기/탄창 객체의 `OnItemModified`가 누락되어 관련 UI가 stale 상태가 될 수 있던 경로를 보완했다.
- 장비 슬롯 탄창 언로드 이벤트 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 일반 인벤토리 탄창 언로드 시 원본 무기·탄창 객체의 `OnItemModified`가 누락되어 관련 UI가 stale 상태가 될 수 있던 경로를 보완했다.
- 일반 인벤토리 탄창 언로드 이벤트 수정 후 실제 Unreal Editor 빌드(컴파일·링크)가 성공했다.
- 세 탄창 언로드 경로의 5자리 난수 기반 탄약 `InstanceID`를 GUID 기반으로 변경해 반복 언로드 시 ID 충돌 가능성을 제거했다.
- 언로드 탄약 ID 충돌 방지 수정 후 실제 Unreal Editor 빌드(UHT·컴파일·링크)가 성공했다.
- 탄창 언로드 시 `Ammo_556_M995`를 하드코딩하던 세 경로를 탄창의 `CompatibleAmmo`와 DataTable 소비재 행을 조회하는 방식으로 변경했다.
- 언로드 탄약 호환 데이터 조회 수정 후 실제 Unreal Editor UHT·컴파일·링크가 성공했다.
- `FItemData::ItemIcon`이 `UItemInstance`로 전달되지 않아 DataTable 아이콘을 사용할 수 없던 경로를 보완하고, 동적 파일 아이콘보다 DataTable 아이콘을 우선 사용하도록 수정했다.
- DataTable 아이콘 전달 수정 후 실제 Unreal Editor UHT·컴파일·링크가 성공했다.
- `AddItem`이 동일 인스턴스 재배치 시 기존 점유 셀을 남겨 중복 위치가 생길 수 있던 상태 불일치 경로를 보완했다.
- `AddItem` 중복 셀 정리 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 적 처치 보상으로 즉시 탈출할 때 발사 탄약이 Stash 저장 전에 차감되지 않을 수 있던 전투 순서를 보완하고, 공격 실패 시 탄약을 복구하도록 수정했다.
- 전투 탄약 차감 순서 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 성공 추출 후 `LootContainerComponent`가 정리되지 않아 레이드 상자 아이템이 남을 수 있던 상태 누락을 보완했다.
- 성공 추출 루팅 컨테이너 정리 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- `SaveStash` 실패 결과를 무시해 추출 아이템을 잃을 수 있던 경로를 보완하고, 저장 성공 확인 전에는 레이드 인벤토리를 비우지 않도록 수정했다.
- 추출 저장 실패 롤백 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 추출 대상 아이템의 `InstanceID`가 기존 Stash에 존재할 때 기존 보관 아이템을 덮어쓸 수 있던 경로를 사전 차단하도록 보완했다.
- 추출 Stash ID 충돌 방지 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- Stash 로드 시 중복 `InstanceID` 레코드가 앞서 복원된 아이템을 덮어쓸 수 있던 경로를 감지하고 중복 레코드를 건너뛰도록 보완했다.
- Stash 로드 중복 ID 방지 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- Stash 로드 중 잘못된 좌표·겹침·중복·부착물 복원 실패가 있어도 `true`를 반환하던 경로를 보완해 부분 복원 결과를 정확히 보고하도록 수정했다.
- Stash 부분 로드 결과 보고 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 레이드 성공·실패 후에도 컨테이너 탐색이 시작될 수 있던 상태 경계를 `InRaid` 조건으로 보완했다.
- 레이드 종료 후 탐색 차단 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 레이드 성공·실패 후에도 미니맵 타일 선택과 전진 버튼으로 이동할 수 있던 상태 경계를 `InRaid` 조건으로 보완했다.
- 레이드 종료 후 이동 차단 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 탐색 완료·식별 지연 콜백이 레이드 종료 또는 전투 시작 후에도 실행되던 상태 경계를 보완해, 해당 콜백에서 대기 큐와 타이머를 정리하도록 수정했다.
- 탐색 타이머 상태 경계 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 루팅 가중치 합계와 추첨 대상에 음수 `DropWeight`가 포함되어 확률을 왜곡할 수 있던 경로를 양수 가중치만 사용하도록 보완했다.
- 루팅 가중치 필터 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 레이드 종료 후에도 판매·외부 호출로 점수와 종료 판정이 다시 변경될 수 있던 경로를 `InRaid` 상태에서만 점수를 반영하도록 보완했다.
- 레이드 점수 상태 경계 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 탄창 언로드 시 대상 탄약 행의 `MaxStack`이 0 이하이면 반복문이 종료되지 않을 수 있던 세 경로를 사전 검증하도록 보완했다.
- 탄약 언로드 유효성 검증 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- Raw PNG 아이콘을 매번 임시 텍스처로 생성해 UI 갱신·GC 후 참조가 끊길 수 있던 경로를 아이템 인스턴스의 transient 캐시로 보관·재사용하도록 보완했다.
- 동적 아이콘 수명·재사용 수정 후 UHT와 실제 Unreal Editor 컴파일·링크가 성공했다.
- `InitializeGrid()`가 0 또는 음수 크기를 그대로 받아 배열·좌표 상태를 깨뜨릴 수 있던 경로를 각 축 최소 1칸으로 보정하도록 수정했다.
- 그리드 크기 입력 검증 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 스택 분할 확정 이벤트에 비정상 수량이 직접 전달될 경우 원본 스택이 음수화될 수 있던 경로를 확정 시 1부터 최대 분할량까지 보정하도록 수정했다.
- 스택 분할 입력 검증 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- `CheckItemFit()`이 0 또는 음수 아이템 크기를 허용해 셀을 점유하지 않는 유령 아이템을 등록할 수 있던 경로를 비정상 크기 거부로 보완했다.
- 아이템 크기 입력 검증 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- `NAME_None` 인스턴스 ID를 가진 아이템이 빈 셀 표기값과 충돌하며 배치될 수 있던 경로를 `AddItem()` 진입점에서 거부하도록 보완했다.
- 아이템 인스턴스 ID 검증 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 현재 변경 상태로 `UnrealEditor-Cmd` 무인·NullRHI 초기화/종료 스모크 테스트가 종료 코드 0으로 완료되어 GameMode 초기화 진입 크래시는 확인되지 않았다.
- Stash 복원 시 저장 레코드의 스택·장탄수 상한을 검증하지 않아 한도를 초과한 인스턴스가 생성될 수 있던 경로를 DataTable의 `MaxStack`·`MaxAmmo` 기준 clamp로 보완했다.
- Stash 수량 상한 검증 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- Stash 로드가 본체 ID만 중복 검사하고 부착물 ID는 검사하지 않아 손상된 세이브에서 동일 부착물이 중복 복원될 수 있던 경로를 전체 인스턴스 ID 집합 검증으로 보완했다.
- Stash 부착물 ID 검증 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 삽탄 경로가 아이템 ID의 `Ammo` 문자열 포함 여부에 의존하던 문제를 실제 아이템 카테고리(`Consumable`) 검사로 통일했다.
- 미니맵 `InitializeMap()`이 0 또는 음수 크기로 맵 배열을 초기화할 수 있던 경로를 각 축 최소 1칸으로 보정하도록 수정했다.
- 미니맵 크기 입력 검증 수정 후 실제 Unreal Editor 컴파일·링크가 성공했다.
- 9개 구역 enum만 지원하는 미니맵에 9보다 큰 크기를 설정하면 구역 값이 범위를 벗어날 수 있던 경로를 각 축 최대 9칸 제한으로 보완했다.
- `SELL ALL`이 DataTable에 없는 아이템까지 전체 인벤토리 초기화로 삭제하거나 레이드 외 상태에서 점수 없이 아이템을 지울 수 있던 경로를 상태 확인과 판매 가능 아이템별 제거로 보완했다.
- `SELL BAG`가 목표 점수 달성에 따른 자동 탈출보다 늦게 판매 아이템을 제거해 해당 아이템이 Stash에 보관될 수 있던 순서를 판매 제거 후 점수 반영으로 수정했다.
- 전투 UI가 적 체력만 표시하고 공격·처치·반격 결과를 남기지 않던 경로를 마지막 전투 메시지 기록 및 기존 CombatText 표시로 보완했다.
- 적 반격 명중 시 체력만 변경되고 전투 메시지 UI 이벤트가 누락되던 경로를 생존 중 전투 상태 브로드캐스트로 보완했다.
- `SELL BAG`가 판매 가능한 아이템 금액만 계산한 뒤 배낭 전체를 비워 DataTable에 없는 아이템까지 삭제할 수 있던 경로를 판매 대상으로 확인된 인스턴스만 제거하도록 보완했다.
- 컨테이너 탐색 완료 시 `bIsExamined`만 변경하고 아이템별 변경 이벤트를 보내지 않아 Inspect/개별 아이템 위젯이 stale 상태로 남을 수 있던 경로를 `OnItemModified` 브로드캐스트로 보완했다.
- `LoadStash()`가 손상된 저장 파일을 읽을 때 실제 Stash를 먼저 초기화해 기존 데이터를 잃을 수 있던 경로를 임시 인벤토리에서 전체 로드 성공을 확인한 뒤 실제 Stash에 반영하도록 원자적으로 보완했다.
- 제한 시간 만료 시 quota 달성 후 추출이 전투·공간 부족으로 실패하면 레이드가 `InRaid`에 고정될 수 있던 경로를 실패 처리로 보완했다.
- 분할 팝업이 열린 뒤 원본 아이템이 이동·삭제되어도 확인 콜백이 수량을 차감할 수 있던 경로를 출처 인벤토리의 동일 인스턴스 보유 여부 확인으로 보완했다.
- `SELL ALL`이 배낭·리그·포켓·금고만 순회해 검색된 루팅 컨테이너 아이템을 남기던 범위를 루팅 컨테이너까지 포함하도록 보완했다.
- Stash 열기·닫기·모드 전환에서 `SaveStash()` 실패를 무시하고 화면을 전환하던 경로를 저장 성공 시에만 전환하도록 보완했다.
- Stash 아이템의 점유 셀을 찾지 못해도 해당 레코드를 조용히 생략하고 저장 성공을 반환하던 경로를 저장 실패로 보고하도록 보완했다.
- Stash 회전 아이템의 실제 크기와 `GridCells` 점유 영역이 어긋난 상태를 저장할 수 있던 경로를 전체 점유 footprint 검증 후 저장하도록 보완했다.
- Stash 복원 시 저장 슬롯 이름만 믿고 잘못된 카테고리·부착물 타입을 장착할 수 있던 경로를 무기·예상 부착물 타입 검증으로 보완했다.
- Stash 세이브의 가로·세로 값에 상한이 없어 손상된 세이브가 과도한 그리드 할당을 시도할 수 있던 경로를 1~10 범위 검증으로 보완했다.
- 해당 방어 코드의 정상 런타임은 검증했으며, 실제 손상 SaveGame 주입 테스트는 읽기 전용 프로퍼티 때문에 미검증으로 남겼다.
- `SaveStash()`가 로드할 수 없는 템플릿·부착 타입·중복 인스턴스 ID를 먼저 저장할 수 있던 경로를 저장 단계에서 거부하도록 보완했다.
- 저장 불가능한 장착물 회귀 자동화 테스트와 기존 프로젝트 자동화 테스트 6건이 모두 통과했다.
- 장착물 템플릿이 존재하더라도 DataTable 카테고리·부착 타입이 실제 장착 위치와 다르면 저장할 수 있던 변형도 저장 단계에서 거부하도록 보완했다.
- 임시 슬롯에서 Stash 아이템 위치·장탄수·회전·식별 상태와 장착 탄창의 ID·장탄수가 저장 후 재로드되는 왕복 회귀 자동화 테스트를 추가하고 통과시켰다.
- 추출 후 Stash 저장이 실패하는 경우 임시로 이동한 아이템만 롤백되고 원본 레이드 인벤토리·레이드 상태가 유지되는 회귀 자동화 테스트를 추가하고 통과시켰다.
- 모드 슬롯 드롭 시 GameMode만 확인하고 인벤토리 컴포넌트를 바로 역참조할 수 있던 경로를 인벤토리 null 검증으로 보완했다.
- 인벤토리가 가득 찬 상태에서 새 모드가 차지하던 칸을 제외하지 않아 기존 모드 교체가 불필요하게 실패할 수 있던 경로를 출처 아이템 제외 공간 탐색으로 보완했다.
- Stash 화면에서 상단 토글로 나갈 때 `SaveStash()`가 호출되지 않아 변경 사항이 저장되지 않을 수 있던 경로를 토글 전 저장으로 보완했다.
- 그리드 아이템 회전 시 실제 점유 셀과 UI가 갱신되지 않고 회전 후 크기가 다른 아이템과 겹칠 수 있던 경로를 현재 위치의 회전 가능 여부 확인 및 인벤토리 갱신 이벤트로 보완했다.
- 스택 분할 이벤트 수신부에서도 원본 수량을 초과하거나 0 이하인 분할값을 허용할 수 있던 경로를 원본 수량 기준 clamp로 보완했다.
- `InitFromData()`가 잘못된 DataTable 크기·스택·장탄수 값을 그대로 복사하거나 재초기화 시 기존 장탄수를 남길 수 있던 경로를 최소 유효값 보정과 장탄수 초기화로 보완했다.
- `EquipItem()`이 빈 슬롯만 확인해 동일 인스턴스 또는 `NAME_None` ID 아이템을 중복 등록할 수 있던 경로를 슬롯·인스턴스 ID 검증으로 차단했다.
- 같은 인벤토리에서 스택 분할 드롭을 원본 아이템이 차지한 위치에 수행할 때 새 인스턴스가 기존 셀과 겹칠 수 있던 경로를 빈 셀 기준 검사로 보완했다.
- `TryMergeItem()`이 동일 인스턴스 또는 0 이하 원본 수량을 직접 병합할 수 있던 경로를 병합 전 입력 검증으로 차단했다.
- 레이드 중 `OPEN STASH`를 통해 아이템을 창고로 옮겨 사망 페널티를 우회할 수 있던 경로를 레이드 외 상태에서만 창고를 열도록 제한했다.
- `GridBoardWidget`이 아이템 포인터가 없는 DragDrop 객체를 역참조할 수 있던 드롭·미리보기 경로를 null 검증으로 보완했다.
- 장비 교체 중 새 아이템 장착이 실패했을 때 복구 대상이 플레이어 인벤토리로 고정되어 출처 아이템이 잘못 이동하거나 유실될 수 있던 경로를 드래그 출처 인벤토리 우선 복구로 보완했다.
- 현재 변경 상태로 `UnrealEditor-Cmd` 무인·NullRHI 런타임 스모크 테스트가 종료 코드 0으로 완료되었고, 프로젝트 로그에서 치명 오류·Assertion·Unhandled Exception이 검출되지 않았다.
- 실제 플레이에 가까운 게임 월드에서 전투 UI 갱신과 전투 수치 결과를 확인하는 회귀 검증까지 통과했으며, 별도 그래픽 수동 플레이 검증은 남아 있다.
- 실제 `/Game/DataTable/DT_ItemData` 에셋에서 M4A1 피해량 25, PACA 방어력 5, M4 탄창 장탄수 30 및 `5.56x45mm` 호환 값이 임포트된 CSV와 일치하는지 런타임 자동화 테스트로 확인했다.
- 실제 게임 월드에서 생성한 `UMainGameUI`가 적 등장·플레이어 피해·반격 실패·적 처치 전투 이벤트를 `CombatText`에 반영하는 UI 회귀 자동화 테스트를 추가하고 통과시켰다.
- 실제 `UMainGameUI::OnBangButtonClicked()` 입력 경로가 장착 탄창의 탄약을 1발 소비하고 무기 피해를 적 체력에 적용하는 `BangHandlerConsumesAmmoAndDealsDamage` 회귀 테스트를 추가했으며 통과했다.
- 인벤토리의 부분 병합·회전 경계·교차 이동 실패 복구와 게임 월드 기반 Inspect 식별 상태 반영을 자동화 테스트로 검증했다.
- 미니맵 초기화가 시작 위치 표시만으로 `OnPlayerMoved`를 발생시켜 첫 이동 전 전투를 만들 수 있던 경로를 분리하고, 확률 100% 조건의 초기 표시 회귀 테스트가 통과했다.
- 이동 진행 중 목적지 클릭이 기존 경로와 진행도를 초기화하던 경로를 차단하고, 이동 중 경로 고정 회귀 테스트가 통과했다.
- 이동 중 적 조우가 발생해도 남은 경로를 보존하고, 전투 종료 후 다음 칸으로 이동을 재개하는 `CombatEndResumesMovement` 회귀 테스트가 통과했다.
- 성공 추출 시 SafeBox 아이템이 Stash 수집 대상에서 빠지고 메모리에만 남던 경로를 Stash 이동 및 성공 후 SafeBox 정리로 보완했으며, 실제 게임 월드 추출 회귀 테스트가 통과했다.
- 초기 실행이 곧바로 `InRaid`로 진입해 Stash에서 로드아웃을 준비하거나 다음 레이드를 시작할 수 없던 흐름을 `Lobby` 초기 화면·`START RAID` 버튼·`StartRaid()` 상태 전환으로 보완했다.
- `StartRaid()`의 월드 없는 호출에서 타이머 매니저를 역참조하던 크래시를 월드 유효성 검사로 보완하고, `LobbyCanStartRaid` 회귀 테스트가 실제 무인 Unreal 실행에서 통과했다.
- 레이드 재시작 시 이전 미니맵 좌표·선택 경로·전진 진행도가 남을 수 있던 상태 누수를 `ResetMovement()`로 초기화하고, `MinimapResetsMovementState` 회귀 테스트가 통과했다.
- 실제 게임 월드의 `SELL BAG` 실행이 아이템 가치와 스택 수량을 곱해 점수에 반영하고, 판매 가능한 아이템만 삭제하는지 회귀 자동화 테스트로 검증했다.
- 점수 획득 시 `AddScore()`가 진행 중인 미니맵 이동을 초기화하지 않도록 위치를 보정하고, `ScoreDoesNotResetMinimapMovement` 회귀 테스트와 전체 19개 테스트가 통과했다.
- 레이드 결과에서 변경된 타이머 색상이 다음 레이드 시작 후에도 남던 상태 누수를 `UpdateTimer()`에서 기본 흰색으로 복원하도록 수정하고, `StartingRaidResetsResultTimerStyle` 회귀 테스트와 전체 22개 테스트가 통과했다.
- 레이드 성공·실패 종료 후 이전 미니맵 경로와 전진 진행도가 남던 상태 누수를 `ExtractRaid()`와 `FailRaid()`에서 `ResetMovement()`로 초기화하도록 수정하고, `TerminalRaidClearsMovementState` 회귀 테스트와 전체 23개 테스트가 통과했다.
- 제한시간이 0초가 된 순간 quota가 충족되어도 성공 처리되던 우선순위 오류를 `CheckWinCondition()`에서 시간 만료를 먼저 처리하도록 수정하고, `TimeoutPrecedesQuota` 회귀 테스트와 전체 24개 테스트가 통과했다.
- Lobby/종료 상태에서도 BANG 핸들러가 탄약을 소비하고 적에게 피해를 줄 수 있던 입력 경계 오류를 `OnBangButtonClicked()`에서 `InRaid` 상태를 확인하도록 수정하고, 기존 전투 테스트에 비전투 차단 검증을 추가해 전체 24개 테스트를 통과했다.
- 미니맵이 초기화되기 전에 타일 클릭 콜백이 실행될 경우 `MapManager` 또는 `AdvanceButton`을 역참조하던 경로를 입력 무시로 보완하고, `MinimapRejectsUninitializedInput` 회귀 테스트와 전체 25개 테스트를 통과했다.
- `MapGrid` 배열이 `MapWidth`·`MapHeight`에 비해 축소된 손상 상태에서 `CanMoveBetween()`과 `GetTileData()`가 범위를 벗어나 접근할 수 있던 경로를 인덱스 유효성 검사로 보완하고, `MapManagerRejectsTruncatedGrid` 회귀 테스트와 전체 26개 테스트를 통과했다.
- `TotalTimeLimit`·`QuotaScore`·`MaxHealth`를 `BeginPlay()`에서 하드코딩해 에디터/Blueprint 설정을 덮어쓰던 문제를 생성자 기본값과 런타임 설정 보존 방식으로 수정하고, `LobbyCanStartRaid` 테스트에서 기본값·사용자 지정 시간의 적용을 검증했다.
- `UCombatComponent`의 Blueprint 호출 가능 전투 API가 로비·종료 상태에서도 적 생성·공격·반격을 허용하던 경계를 `InRaid` 상태 검사로 보완하고, `CombatApiRejectsNonRaidState` 회귀 테스트를 추가했다.
- 전투 API 상태 가드 도입 후 기존 BANG 회귀 테스트가 로비에서 적을 생성하던 전제와 충돌한 것을 확인해, 적 생성은 레이드 중에 수행하고 로비 전환 후 공격 차단을 검증하도록 테스트 순서를 고쳤다.
- 아이콘 표시 경로는 `ItemIcon` Soft Reference와 `RawAssets/Icons/<TemplateID>.png` 순서로 연결되어 있으나, 현재 프로젝트에 아이콘 파일과 CSV 아이콘 경로가 없어 콘텐츠 에셋 준비 전까지 미검증 상태로 남긴다.
- `GridBoardWidget`이 그리드 크기와 실제 `GridCells` 배열 길이가 불일치할 때 드래그 원위치 탐색·UI 갱신에서 범위를 벗어나 접근할 수 있던 경로를 인덱스 유효성 검사로 보완하고, 축소된 셀 배열을 사용한 UI 회귀 테스트를 추가했다.
- `InitFromData()`가 기존 인스턴스를 재초기화할 때 회전·식별·무기 부착물 상태를 남길 수 있던 상태 누수를 초기화하도록 보완하고, 재사용 인스턴스 회귀 테스트를 통과시켰다.
- 전투·로비·레이드 종료 중에도 레이드 전용 UI 버튼이 시각적으로 활성화되어 잘못된 입력을 유도하던 경로를 상태 이벤트 기반 버튼 활성화 갱신으로 보완하고, 상태별 액션 버튼 회귀 테스트를 통과시켰다.
- 일반 인벤토리 아이템의 컨텍스트 메뉴에 동작하지 않는 Unequip 항목이 표시되던 경로를 장비 슬롯·모드 슬롯에서만 표시하도록 제한하고, 일반 아이템 메뉴의 Unequip 숨김 회귀 테스트를 통과시켰다.
- GameMode가 없는 미니맵 위젯에서 전진 입력이 레이드 상태 확인 없이 진행될 수 있던 경계를 입력 거부로 보완하고, `MinimapRejectsAdvanceWithoutGameMode` 회귀 테스트와 전체 32개 테스트를 통과시켰다.
- Stash에 저장된 기본 장비·데모 아이템과 다음 실행 때 생성되는 고정 인스턴스 ID가 충돌할 수 있던 경로를 기존 ID 사용 시 GUID 기반 ID로 대체하도록 보완하고, `UniqueInstanceIDAvoidsSavedCollision` 회귀 테스트와 전체 33개 테스트를 통과시켰다.
- GameMode가 없는 미니맵에서 타일을 선택해 이동 경로를 만들 수 있던 입력 경계를 레이드 상태 확인으로 보완하고, `MinimapRejectsTileSelectionWithoutGameMode` 회귀 테스트와 전체 34개 테스트를 통과시켰다.
- 전투 중 전진 입력은 차단되지만 타일 선택으로 목적지 경로를 바꿀 수 있던 경계를 전투 상태 검사로 보완하고, `MinimapRejectsTileSelectionDuringCombat` 회귀 테스트와 전체 35개 테스트를 통과시켰다.
- 장비 슬롯·그리드·모드 슬롯 간 부착물 이동 시 출처 무기에서 부착물을 분리하지 않거나, 드래그 시작 때 이미 분리된 `SourceModSlot` 출처를 잘못 거부해 이동이 실패할 수 있던 경로를 공통 분리 처리와 실패 시 롤백으로 보완하고, `RemovesAttachedItemFromAllWeapons` 회귀 테스트와 전체 36개 테스트를 통과시켰다.
- 모드 슬롯 드래그 시작 후 UI 갱신으로 부착물 포인터가 초기화되어 드래그 비주얼 생성과 크기 계산에서 널 포인터가 사용될 수 있던 경로를 드래그용 포인터 보존으로 보완하고, 전체 36개 테스트를 통과시켰다.
- 월드가 없는 미니맵 위젯이 `GetGameMode(this)`를 호출해 입력 거부 자체는 정상이어도 엔진 경고를 남기던 경로를 유효한 월드에서만 GameMode를 조회하도록 보완하고, 관련 4개 테스트와 전체 36개 테스트를 통과시켰다.
- 레이드 종료 직전에 이미 예약된 타이머 콜백이 실행되면 `Lobby/Succeeded/Failed` 상태에서도 남은 시간이 감소할 수 있던 경계를 `InRaid` 상태 가드와 타이머 해제로 보완하고, `TimerIgnoresTerminalState` 회귀 테스트 및 전체 37개 테스트를 통과시켰다.
- 미니맵 `InitMinimap()`이 플레이어 진행도만 초기화하고 이전 목적지·경로를 남길 수 있던 상태 누수를 목적지와 경로까지 함께 초기화하도록 보완하고, `MinimapReinitializationClearsMovementState` 회귀 테스트 및 전체 38개 테스트를 통과시켰다.
- Stash 등 다른 인벤토리로 드래그할 때 대상 추가 후 출처 제거가 실패해 아이템이 양쪽에 남을 수 있던 경계를 `RemoveItem()`의 성공 결과 반환과 대상 롤백으로 보완하고, `RemoveItemReportsMissingPlacement` 회귀 테스트 및 전체 39개 테스트를 통과시켰다.
- 장비·모드 슬롯으로 부착물을 퀵 모딩할 때 출처 포인터 확인만으로 제거 성공을 판정해 비정상 출처의 부착물이 중복 장착될 수 있던 경계를 `RemoveItem()` 반환값으로 확인하도록 보완했다.
- 탄약이 모두 소진되는 삽탄 드래그에서 탄창·스택을 먼저 변경한 뒤 출처 제거 실패를 무시할 수 있던 경계를 제거 결과 확인과 탄창·스택 롤백으로 보완했다.
- 완전 스택 병합에서 대상·출처 수량 변경 후 출처 제거 실패를 무시해 상태가 어긋날 수 있던 경계를 제거 결과 확인과 양쪽 스택 롤백으로 보완하고, 장비 인스턴스 제거 결과도 반환하도록 정리했다.
- 부분 스택 병합 전에 오래된 드래그의 출처 인벤토리·아이템 포인터와 ID를 확인하지 않아 출처가 없는 아이템이 병합될 수 있던 경계를 사전 검증으로 차단했다.
- 장비에서 그리드로 드래그할 때 대상 추가 후 장비 슬롯 제거 실패를 무시해 아이템이 양쪽에 남을 수 있던 경계를 슬롯 제거 결과 확인과 대상 롤백으로 보완하고, 슬롯 제거 성공·실패 반환 테스트를 추가했다.
- 장비·모드 슬롯 출처를 찾지 못한 오래된 드래그가 그리드에 새 아이템으로 추가될 수 있던 경계를 유효한 `SourceModSlot` 또는 장비·인벤토리 출처가 확인될 때만 수용하도록 보완했다.
- 장비 해제 시 오래된 UI가 가리키는 아이템을 현재 슬롯 소유물로 확인하지 않거나 장비 제거 실패를 무시하던 경계를 슬롯 소유권 확인과 제거 결과 검증으로 보완했다.
- 장비 슬롯 간 교체 실패 시 출처 장비가 인벤토리로 잘못 복구될 수 있던 경계를 드래그 출처 슬롯 전달과 원래 장비 슬롯 복구로 보완했다.
- 장비 교체 시 기존 슬롯 제거 실패를 확인하지 않고 기존 장비 수납을 진행하던 경계를 슬롯 제거 성공 확인으로 보완했다.
- 장비 교체 시 새 장비를 모든 인벤토리에서 ID로 무조건 제거하고 출처 제거 결과를 무시하던 경계를 출처 소유권 확인 및 정확한 제거 결과 검증으로 보완했다.
- 장비 교체 중 기존 장비 수납·출처 제거가 실패했을 때 아직 제거되지 않은 새 아이템까지 복구하거나 기존 장비 제거 결과를 무시하던 경계를 실제 제거 여부에 따른 복구와 반환값 확인으로 보완했다.

완료 기준:

- 적 인카운터가 재현 가능함
- 한 턴의 공격·피격·탄약·피해 결과가 일관됨
- 전투 사망 결과가 Phase 5의 사망 페널티와 연결됨

### 단계 D — Phase 7: Economy

1. 화폐와 플레이어 잔액 데이터 추가
2. 상점 아이템 목록과 구매 기능 구현
3. 판매 가격을 `FItemData::Value`와 스택 수량에 맞게 계산
4. Stash와 상점 간 거래 흐름 구현
5. 구매·판매·잔액 변경 회귀 테스트

## 7. 작업 및 보고 방식

- 문제를 하나씩 수정한다.
- 각 문제마다 수정 파일, 원인, 검증 결과, 남은 위험을 보고한다.
- Discord에는 Goal 진행 상황, 사용자 도움이 필요한 작업의 보류 내용, 완료·오류, 사용자가 요청한 보고만 전송한다. 일반 질의응답은 전송하지 않는다.
- Codex의 정확한 토큰 수량은 제공되지 않으므로, 확인 가능한 계정 사용률과 소요 시간을 함께 기록한다.
- 컴파일 도구나 에디터가 없어 검증할 수 없는 경우, 정적 검증과 미검증 범위를 분리해 보고한다.

## 8. 현재 다음 작업

이번 턴 수정:

- 장비 슬롯의 오래된 언로드 메뉴가 현재 슬롯에 없는 아이템의 탄창·탄약 상태를 변경할 수 있던 경계를 UI 포인터와 실제 장비 슬롯 소유권 확인으로 차단했다.
- 모드 슬롯의 오래된 언로드 메뉴가 현재 장착되지 않은 무기·탄창의 탄약을 추출할 수 있던 경계를 실제 장비 무기와 탄창 포인터 확인으로 차단했다.
- 모드 슬롯의 오래된 해제 메뉴가 실제 장비가 아닌 무기의 부착물을 인벤토리로 옮기고 포인터를 해제할 수 있던 경계를 장비 무기와 부착 위치 소유권 확인으로 차단했다.
- 인벤토리 아이템의 오래된 언로드 메뉴가 출처 인벤토리에서 이미 제거된 무기·탄창의 상태를 변경할 수 있던 경계를 출처 인벤토리의 실제 아이템 포인터 확인으로 차단했다.
- 인벤토리 아이템의 오래된 폐기 메뉴가 같은 ID를 가진 현재 아이템을 잘못 삭제할 수 있던 경계를 출처 인벤토리의 실제 아이템 포인터 확인으로 차단했다.
- 오래된 인벤토리 드래그가 같은 ID의 현재 아이템을 출처에서 제거하거나 낡은 포인터를 대상 인벤토리에 추가할 수 있던 경계를 실제 출처 포인터 확인과 제거 성공 검증으로 차단했다.
- 모드 슬롯의 오래된 더블클릭 해제가 실제 장비가 아닌 무기의 부착물을 인벤토리로 옮기고 포인터를 지울 수 있던 경계를 장비 목록과 실제 부착 슬롯 포인터 확인으로 차단했다.
- 모드 드래그 취소 시 원래 무기가 교체되거나 슬롯이 다른 부착물로 채워졌는데도 기존 부착물이 덮어써질 수 있던 경계를 실제 장비·슬롯 확인과 인벤토리 보존 경로로 보완했다.
- 장비 슬롯 UI가 초기화되기 전에 드래그가 시작되면 `ItemCanvas->GetChildAt(0)`를 역참조할 수 있던 경계를 초기화 여부 확인으로 차단했다.
- 이미 다른 객체가 사용 중인 `InstanceID`로 인벤토리에 아이템을 추가할 때 기존 객체가 덮어써질 수 있던 경계를 중복 ID 거부와 회귀 테스트로 차단했다.
- 전투 중 새 적 스폰이 현재 적의 체력·보상을 덮어쓸 수 있던 경계를 활성 적 스폰 거부와 회귀 테스트로 차단했다.
- 그리드 셀이 존재하지 않는 아이템 ID를 가리킬 때 드롭 처리에서 null 아이템을 역참조할 수 있던 경계를 잘못된 셀 참조 거부와 실제 드롭 회귀 테스트로 차단했다.
- 오래된 인벤토리 아이템 위젯이 같은 ID의 현재 아이템 위치를 이용해 분리된 객체의 회전 상태를 바꿀 수 있던 경계를 출처 포인터 확인과 stale 회전 회귀 테스트로 차단했다.
- 오래된 탄약 드래그가 일부 충전 경로에서 탄창과 분리된 탄약 객체의 수량을 바꿀 수 있던 경계를 그리드·장비·모드 슬롯의 출처 포인터 확인과 stale 탄약 드롭 회귀 테스트로 차단했다.
- 오래된 인벤토리 검사 메뉴가 분리된 아이템을 검사 완료 처리할 수 있던 경계를 출처 포인터 확인과 stale 검사 회귀 테스트로 차단했다.
- 오래된 장비 슬롯 검사 메뉴가 현재 슬롯에 없는 아이템을 검사 완료 처리할 수 있던 경계를 슬롯 아이템 포인터 확인과 회귀 테스트로 차단했다.
- 식별자 없는 적 정의가 활성 전투로 등록되어 UI·보상·로그의 전투 상태를 오염시킬 수 있던 경계를 `EnemyID` 유효성 확인과 회귀 테스트로 차단했다.
- 장비 컴포넌트가 초기화되지 않은 상태에서 사망 처리될 때 `FailRaid`가 장비 제거를 위해 null 포인터를 역참조할 수 있던 경계를 null 확인과 회귀 테스트로 차단했다.
- 월드가 없는 복구·자동화용 GameMode에서 `FailRaid`가 타이머 매니저를 무조건 참조할 수 있던 경계를 월드 확인과 회귀 테스트로 차단했다.
- 월드가 없는 GameMode에서 추출 성공 후 `ExtractRaid`가 타이머 매니저를 무조건 참조할 수 있던 경계를 월드 확인과 추출 회귀 테스트로 차단했다.
- 빈 아이템 ID로 `RemoveItem`을 호출할 때 빈 그리드 셀까지 제거 성공으로 처리될 수 있던 경계를 `NAME_None` 입력 거부와 회귀 테스트로 차단했다.
- 레이드 상태만 종료 상태로 바뀐 경우 전투 텍스트가 이전 적을 계속 표시할 수 있던 경계를 상태 이벤트 연결과 레이드 상태 확인으로 차단했다.
- 장비 컴포넌트를 사용할 수 없는 순간 장비 슬롯 위젯이 이전 아이템을 계속 표시하던 경계를 빈 슬롯으로 명시적으로 갱신하고 회귀 테스트로 고정했다.
- 실제 그리드 셀에서 제거되지 않은 판매 아이템까지 점수에 포함될 수 있던 경계를 `RemoveItem()` 성공분만 집계하도록 수정하고 일반 판매·전체 판매 회귀 테스트로 고정했다.
- 월드가 없는 GameMode에서 타이머가 만료되거나 이미 종료된 상태를 갱신할 때 `GameTimerUpdate()`가 타이머 매니저를 null 역참조할 수 있던 경계를 모든 정리 경로의 월드 확인과 worldless 회귀 테스트로 차단했다.
- 이동 경로 생성 후 맵의 벽 정보가 바뀌어도 경로 좌표만 믿고 막힌 칸으로 이동할 수 있던 경계를 실제 인접 이동 가능성 재검증과 stale blocked-step 회귀 테스트로 차단했다.
- 스택 분할로 생성된 새 아이템에 `ItemIcon`과 이미 생성된 `CachedDynamicIcon`이 복사되지 않아 아이콘이 사라질 수 있던 경계를 두 분할 경로의 아이콘 상태 복사와 회귀 테스트로 차단했다.
- 부착물이 연결된 비정상 스택 아이템을 분할할 때 두 인스턴스가 같은 부착물 포인터를 공유할 수 있던 경계를 부착물 보유 스택 분할 거부와 회귀 테스트로 차단했다.
- Stash 저장 시 선언된 그리드 크기와 셀 배열이 다르면 잘못된 좌표를 저장할 수 있던 경계를 shape·footprint 검증과 malformed grid 저장 거부 회귀 테스트로 차단했다.
- 인레이드 중 안전지대 Stash에 접근할 수 있던 UI·호출 경계를 Stash 버튼 비활성화, 직접 호출 거부, 레이드 시작 시 패널 자동 전환으로 보완하고 로비·인레이드·종료 상태 회귀 검증을 추가했다.
- 인레이드 Stash 접근 제한 수정에서 테스트 파일의 `UWidgetSwitcher` 헤더 누락을 보완한 뒤 Unreal Development Editor 빌드가 성공했다.
- 부착물이 연결된 비정상 스택 분할 거부 회귀 테스트와 인레이드 Stash 접근 제한 회귀 테스트를 포함한 전체 `GridLootMaster` 자동화 테스트 62개가 통과했다.
- 시작점 반대쪽 외곽 탈출점 생성, 최소 거리·경로 검증, 탈출점 밖 추출 거부를 구현하고 전체 `GridLootMaster` 자동화 테스트 62개가 통과했다.
- 인레이드 하단 축소 미니맵을 추가하고, 전체 미니맵을 단일 이동 상태 원본으로 유지한 채 하단 미니맵의 타일 선택·전진 입력을 위임하도록 구현했다. 공유 상태 자동화 테스트와 전체 `GridLootMaster` 자동화 테스트 63개가 통과했다.
- 화면 상단에 이벤트 알림 바를 추가하고 기존 전투 메시지 및 레이드 성공·실패 결과를 연결했다. 적 출현 메시지가 UI 자동화 테스트에서 표시되는 것을 확인했다.
- 목적지 선택 전에도 전진 버튼을 표시하되 비활성화하고 `목적지 선택 후 전진`으로 안내하도록 보완했다. `MainGameUI`의 토글·목적지 선택·전진 버튼 동기화 흐름을 회귀 테스트로 고정했다.
- Stash 패널의 `BACK TO RAID` 버튼을 제거하고 로비에서는 `START RAID`만 남겼다.
- Stash 화면에서 전역 `Toggle Map/Inv` 버튼을 숨기고, `START RAID`로 레이드에 진입하면 다시 표시하도록 보완했다. 로비 Stash 상태와 기존 레이드 액션 버튼 회귀 테스트를 통과했다.
- 미니맵 장식 요소가 타일 클릭을 가로채 목적지 선택이 무반응처럼 보일 수 있던 경계를 시각 요소의 클릭 통과 처리와 타일 위젯의 직접 마우스 이벤트로 보완하고, 목적지 경로 선택 회귀 테스트를 통과했다.
- 레이드 시작 때 화면에 이미 붙은 미니맵의 `WidgetTree`를 교체해 화면의 Slate 위젯과 갱신 대상이 분리되던 원인을 기존 맵 위젯 재사용과 타일 데이터 갱신으로 수정하고, 재초기화·타일 클릭 회귀 테스트 및 전체 63개 테스트를 통과했다.
- 목적지 선택 및 이동 거부가 무반응처럼 보이지 않도록 이동 메시지 delegate를 상단 알림에 연결하고, `UIFlow` 및 전체 63개 테스트를 통과했다.
- 상단 이벤트 알림이 연속 메시지를 덮어쓰지 않도록 2.5초 순차 큐를 추가하고, `UIFlow`와 전체 63개 테스트를 통과했다.
- 타일 내부 Overlay의 시각 요소를 `HitTestInvisible`로 설정하고 타일 위젯의 `NativeOnMouseButtonDown()`에서 목적지 선택을 직접 처리하도록 변경해, 실제 Slate 마우스 이벤트가 부모 타일에 도달하도록 보완했다. Unreal 빌드·미니맵 공유 상태 테스트·전체 66개 자동화 테스트를 통과했다. 실제 마우스 입력은 Editor 수동 확인이 남아 있다.
- 플레이어가 탈출 타일에 도착할 때 이동 delegate를 통해 `EXTRACT RAID` 상태를 즉시 갱신하고 상단에 `탈출 지점에 도착했습니다.` 알림을 표시하도록 연결했다. `ActionButtonsReflectRaidState`와 전체 63개 자동화 테스트를 통과했다.
- 컨테이너 탐색 시작·완료·아이템 없음 상태를 상단 알림으로 표시해 탐색 중 빈 그리드가 무반응처럼 보이지 않도록 보완했다.
- 컨테이너 탐색 중 중복 입력이 기존 타이머와 분석 큐를 재시작하던 경계를 탐색 중 재입력 거부와 안내 알림으로 보완했다.
- 미니맵 이동 초기화가 설정된 `MapManager->SpawnPoint` 대신 `(0,0)`으로 돌아가던 상태 오류를 수정하고, 비기본 스폰 좌표 회귀 테스트를 통과시켰다.
- 새 레이드 시작 시 이전 레이드의 `전투가 종료되었습니다.` 메시지가 상단 알림으로 재노출될 수 있던 상태 누수를 전투 메시지 초기화와 `LobbyCanStartRaid` 회귀 검증으로 보완했다. Unreal 빌드와 전체 63개 자동화 테스트를 통과했다.
- 새 레이드 시작 시 이전 레이드 결과 배너(`RAID FAILED`/`RAID EXTRACTED`)와 알림 큐가 남던 상태 누수를 초기화하고, Unreal 빌드·StartRaid 전용 테스트·전체 63개 자동화 테스트를 통과했다.
- Loot DataTable 누락·빈 가중치로 탐색이 종료될 때 완료 알림 없이 멈추던 경로에 실패 안내를 추가하고 `SearchFailureShowsNotification` 회귀 테스트와 전체 64개 자동화 테스트를 통과했다.
- 상단 알림 텍스트만 표시되고 숨겨진 부모 배너가 계속 유지되던 UI 가시성 오류를 부모 `UBorder` 상태 동기화로 수정하고 `UIFlow`·전체 64개 자동화 테스트를 통과했다.
- 이동 실패 시 원본 미니맵만 경로를 지우고 축소 미니맵에 이전 경로가 남을 수 있던 동기화 누수를 실패 경로의 상태 발행으로 수정하고, 공유 상태 회귀 테스트와 전체 64개 자동화 테스트를 통과했다.
- 레이드 종료 후에도 큰 맵이 남아 이동 불가 상태가 무반응처럼 보이던 UI 흐름을 종료 상태에서 인벤토리 화면으로 자동 복귀하도록 수정하고, 큰 맵 자동 닫힘·축소 맵 숨김 회귀 테스트와 전체 64개 자동화 테스트를 통과했다.
- 종료·로비 상태에서 `Toggle Map/Inv`로 이동 맵을 다시 열 수 있던 경계를 토글 버튼 숨김과 상태 가드로 차단하고, 종료 상태 토글 숨김 회귀 검증을 추가했다.
- 전투 이벤트 알림을 영어 문자열에서 한국어 문구로 통일해 적 출현 시 `적이 나타났다!!`와 피해·반격·처치 결과가 상단 HUD에 표시되도록 보완했다.
- 탐색 타이머 콜백이 월드·메인 UI 수명을 확인하지 않고 접근하던 런타임 경계를 월드·UI 가드와 `SearchCompletionWithoutWorldIsSafe` 회귀 테스트로 차단했다.
- 탐색 수명 가드 추가 후 전체 `GridLootMaster` 자동화 테스트 65개가 통과했다.
- 손상된 탐색 컨테이너 그리드가 잘못된 랜덤 좌표 범위를 만들 수 있던 경계를 shape 검증·사용 불가 알림과 `SearchRejectsMalformedContainer` 회귀 테스트로 차단했다.
- 컨테이너 shape 방어 추가 후 전체 `GridLootMaster` 자동화 테스트 66개가 통과했다.
- 미니맵 타일의 `USizeBox`와 시각 요소를 전체 영역으로 정렬하고 직접 좌클릭 이벤트를 사용해 표시되는 한 칸 전체가 클릭 히트 영역이 되도록 보완했다.
- 추출 성공 HUD가 버튼·점수 조건 호출부에 분산되던 경로를 `ExtractRaid()` 내부의 단일 성공 처리로 통합해 공개 API 직접 호출에서도 결과 UI가 갱신되도록 보완했다.
- 이동 중 벽 변경으로 경로가 무효화될 때 경로만 사라져 무반응처럼 보이던 상황에 재선택 안내 알림을 추가했다.
- 전투 중 무기·탄창·탄약·피해량이 유효하지 않아 `BANG!` 입력이 무반응처럼 보이던 경로에 원인별 HUD 알림을 추가하고, 빈 탄창 회귀 검증을 보강했다.
- `EXTRACT RAID`가 탈출점 밖에서 무반응처럼 보이던 경로에 조건별 HUD 알림을 추가하고, 실제 GameMode API의 실패 알림 회귀 검증을 보강했다.
- `SEARCH CONTAINER`가 레이드 외부·전투 중·컨테이너 누락에서 무반응처럼 보이던 경로에 조건별 HUD 알림을 추가하고, 로비 검색 시도 회귀 검증을 보강했다.
- 소형 미니맵이 공유 경로를 받은 뒤 GameMode가 없어도 전진 버튼을 활성화할 수 있던 상태 오류를 GameMode 확인 기본값을 비활성으로 변경하고, `MinimapRejectsAdvanceWithoutGameMode` 회귀 테스트와 전체 66개 테스트를 통과시켰다.
- 실제 Slate 클릭 이벤트가 타일 UserWidget까지 도달하지 않는 환경을 보완하기 위해 타일 전체 크기의 투명 `UButton`을 표준 입력면으로 생성하고 `OnClicked`를 `TriggerClick()`에 연결했으며, Combat 25개·전체 66개 자동화 테스트를 통과시켰다.
- 미니맵 그리드 패널이 자기 자신으로 입력을 소비하지 않도록 `SelfHitTestInvisible`로 설정하고, 타일 버튼이 놓이지 않은 경우에도 캐시된 타일 기하를 기준으로 부모가 좌클릭을 보조 처리하도록 보완했으며 Unreal 빌드·Combat 25개·전체 66개 테스트를 통과시켰다.
- Chest Rig 장비 슬롯을 `128x64`(2x1)로 줄이고, Backpack 장비 슬롯은 요청 기준인 `128x192`(2x3)로 유지했다. 백팩 내부 수납 그리드 `5x6`은 변경하지 않았다.
- 성공 추출 시 장비·운반품을 현재 위치에 보존하고 자동 Stash 이동을 제거했으며, `Lobby` 복귀·Stash 패널·START RAID 재활성화와 결과 알림의 일시 표시를 적용했다. 관련 추출 테스트를 새 보존 규칙에 맞춰 갱신했다.
- 사망·시간초과 후 `Failed` 상태에 남아 START RAID가 비활성화되던 흐름을 실패 결과 표시 후 `Lobby` 복귀로 통일하고, 레이드 회귀 테스트 9개를 새 상태 규칙에 맞춰 검증했다.
- 실패 후 실제로 다음 `START RAID`가 실행되는 회귀 테스트를 추가하고, Stash 테스트 10개를 통과시켰다.
- Stash 저장 슬롯을 GameMode 프로퍼티로 분리하고 저장·로드 회귀 테스트마다 GUID 기반 임시 슬롯을 사용하도록 보완해, 사용자 저장 슬롯을 덮어쓰지 않으면서 Stash 3개와 레이드 시작 1개를 포함한 전체 68개 테스트를 실제 실행했다.
- 로비에서 Stash 아이템이 변경될 때 자동 저장하고 저장 실패를 상단 알림으로 표시하도록 연결했으며, `AutosavesInventoryChangesInLobby` 회귀 테스트와 전체 68개 테스트를 통과했다.
- 목적지 `(X, Y)` 표기가 0부터 시작하는 맵 열·행 좌표라는 점과, 향후 구역명 표기로 개선할 후보를 문서화했다.
- 적 인카운터 확장 기획으로 턴/시간 주기 스폰과 탐색 능력 기반 주변 범위 감지·비정확 알림 규칙을 추가했다.
- 전투 개편 C1에서 Random Encounter 확률 판정과 이동 직후 즉석 적 생성을 제거했다.
- 전투 개편 C2에서 `UEnemyManagerComponent`와 `FEnemyWorldInstance`를 추가해 적 고유 ID·좌표·점유·레이드 생명주기 초기화를 관리한다.
- 전투 개편 C3에서 전진 입력을 `AGridGameMode::AdvanceRaidWorldTick()`에 연결하고, 초기 안전 Tick·Seed 기반 Spawn Scheduler·최대 생존 적 수·최소 거리·탈출 타일 제외를 구현했다.
- 전투 개편 C4에서 `RandomWander`가 벽·플레이어·적 점유를 피하면서 유효한 이웃 Tile로 이동하고, 좌표·점유·방향·상태를 갱신하도록 구현했다.
- 전투 개편 C5에서 Chebyshev 타일 거리·벽/대각선 코너 LOS, Hidden/Suspected/Revealed 지식 상태, 탐지 조건 기반 Contact 및 전투 중 월드 Tick 정지를 구현했다.
- 전투 개편 C6에서 플레이어 공격 쿨다운, 적 독립 공격 스케줄, Reaction Time, Combat Range·LOS 거부, 승인된 빗나감의 탄약 소비와 월드 적 처치 동기화를 구현했다.
- 전투 개편 C7의 1차 단위에서 `EWeaponAttackType`과 무기 전투 스탯을 `FItemData`·`UItemInstance`에 추가하고, Firearm/Melee 탄약 규칙과 기존 행 안전 기본값을 연결했다.
- C7 반동 누적·시간 회복을 연결하고, C1~C6 및 C7 스키마·반동 변경 컴파일, Targeted Combat 테스트 30개, 전체 `GridLootMaster` 79개 테스트가 통과했다.
- C8 최소 골격에서 전투 중 무기 교체 지연, 교체·재장전 중 공격 차단, 현재 장착 탄창 기반 재장전과 완료 시점 탄약 커밋을 연결했다. `MainGameUI`의 `R` 입력도 `RequestReload()`에 연결했다. Targeted Combat 32개와 전체 `GridLootMaster` 83개 테스트가 통과했다.
- C9 최소 골격에서 플레이어 `Normal/Ambushing` 자세, 매복 대기·통과·습격 API, 의심 대상 선제 접촉, 적 탐지에 의한 매복 해제를 연결했다. Targeted EnemyWorld 8개와 전체 `GridLootMaster` 83개 테스트가 통과했다.
- 아이템 전투 스탯 원본 CSV가 현재 프로젝트에 없어 실제 DataTable 수치 재임포트는 다음 자료 수령 후 진행한다.

현재 우선순위는 다음과 같다.

1. C7 무기 공격 수치·사거리·반동 구현 및 DataTable 검증
2. 실제 Editor에서 큰 맵·하단 미니맵 타일 클릭과 전진 버튼 수동 확인
3. Chest Rig `128x64`·Backpack `128x192` 장비 슬롯 및 탈출 후 로비 Stash 로드아웃 실제 화면 확인
4. 전투·탐색·탈출·사망·시간초과를 포함한 전체 레이드 그래픽 수동 회귀 테스트
5. 아이콘 에셋 준비 후 아이콘 표시 검증
6. 특수 탈출구 규칙(가방·비용·고정 방향) 구현

사용자 수동 검수 항목과 제공이 필요한 자료는 `USER_VERIFICATION_MEMO.md`에 기록한다.
